<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeakCanaryDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LeakCanaryDemo"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StaticLeakActivity"
            android:exported="false"
            android:label="Static Leak Demo" />

        <activity
            android:name=".ListenerLeakActivity"
            android:exported="false"
            android:label="Listener Leak Demo" />

        <activity
            android:name=".HandlerLeakActivity"
            android:exported="false"
            android:label="Handler Leak Demo" />

        <activity
            android:name=".CoroutineLeakActivity"
            android:exported="false"
            android:label="Coroutine Leak Demo" />
    </application>

</manifest>